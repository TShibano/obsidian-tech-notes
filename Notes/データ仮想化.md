---
title: "データ仮想化"
date: 2026-05-03
tags:
  - DB
  - Cloud
related:
  - "[[データアーキテクチャ]]"
  - "[[データ基盤]]"
  - "[[Apache Iceberg]]"
  - "[[データファブリック]]"
  - "[[データメッシュ]]"
  - "[[データフェデレーション]]"
  - "[[フェデレーテッドクエリ]]"
---

## 概要

データ仮想化（Data Virtualization）はデータを物理的にコピーや移動することなく，複数の異なるデータソースを統合した仮想ビューを作成し，統一インターフェースでクエリを実行するデータ統合手法．DWH・データレイク・データベース・API・SaaS など分散した異種データソースを横断するフェデレーテッドクエリを可能にし，データ移動コストとリアルタイム性のトレードオフを解消する．

## 詳細

### 基本概念

```
利用者（BI ツール・アプリ・AI）
          ↓ 統一された SQL / API
仮想化レイヤー（クエリを分解・最適化）
  ↙         ↓         ↘
DWH    データレイク    SaaS（CRM 等）
（Snowflake）（S3）    （Salesforce）
```

データのコピーを作らず，クエリを各ソースに分解して実行し結果をマージする「フェデレーテッドクエリ（Query Federation）」が中核技術．

### フェデレーテッドクエリの仕組み

1. ユーザーが仮想テーブルに対して SQL を発行
2. データ仮想化エンジンがクエリを解析・最適化
3. 各サブクエリを適切なソースシステムに送信（プッシュダウン）
4. 各ソースの結果をマージして返す

**クエリプッシュダウン**: WHERE 句や JOIN 条件をソース側で実行させることで転送データ量を削減．

### データ仮想化 vs データ移動(データレイクなど)

| 観点             | データ仮想化                         | データ移動                                |
| -------------- | ------------------------------ | ------------------------------------ |
| 開発・運用コスト       | ストレージコスト最小化．<br>定期的な保守・運用管理を削減 | ELTジョブの構築と保守にコスト．<br>ストレージコストも二重にかかる |
| ソリューション展開までの時間 | いてレーションとプロトタイプを高速にできる          | ジョブの構築と実行に時間を要する                     |
| セキュリティ         | データを単一の場所に保管し，攻撃対象領域を最小化       | データのコピーを作成すると情報漏洩リスクが高まる             |
| ガバナンス          | データ移動の範囲を最小限にとどめられる            | 想定外の領域にデータ移動されることが起こりやすい             |
| リアルタイム性        | 高い（ソース直接参照）                    | 低い（バッチ依存）                            |
| クエリ性能          | ソース次第で不安定                      | 事前最適化で高速                             |
| 複雑な変換          | 難しい                            | 容易                                   |

### データ仮想化を採用したくなる場面
- データ移動に法的規制がある場合
	- 国外にデータを持ち出せない場合，仮想化で対応する
- 社外のエンドユーザに顧客データへのアクセスを提供する場合
	- 直接クエリを実行させることで，アクセスログを取得でき，容易にトラッキングできる
- データウェアハウス外部のデータベースに頻繁に変更されるデータがある場合
	- クエリ発行時に最新のデータを使えるようになる
- 様々なデータストアからデータをクエリし結合したい場合
	- RDB，オブジェクトストレージ，NoSQLなどを効率的に処理したい場合
- ユーザに仮想サンドボックスを提供し，セルフサービス分析を提供したい場合
	- ユーザが安全に操作できる仮想サンドボックス内部でクエリを実行することで，データをコピーせずに使用できる
- ソリューションを迅速に構築したい場合
	- ETLツールの開発が不要なため，

### 主要ツール（2026）

| ツール | 特徴 |
|--------|------|
| **Denodo** | 純粋データ仮想化のリーダー．インテリジェントクエリ最適化・動的キャッシュ |
| **Databricks Lakehouse Federation** | Databricks 上でのフェデレーテッドクエリ基盤 |
| **IBM Data Virtualization（Watson Query）** | IBM クラウドエコシステム向け |
| **Domo** | 数百のデータソースへのフェデレーテッド接続 |
| **Starburst/Trino** | オープンソースベースの分散 SQL エンジン |

### データファブリックとの関係

[[データファブリック]] はデータ仮想化を主要な構成要素の一つとして採用する．データファブリックがメタデータ・ガバナンス・オーケストレーション全体を包含するのに対し，データ仮想化はその「統合・クエリ実行」レイヤーを担う．

### 2026 年の戦略パターン

実践的なアプローチとして「バーチャルファースト・マテリアライズオンデマンド」が台頭:

1. まず全データソースを仮想接続（コピーなし）で繋ぐ
2. クエリ頻度・パフォーマンスを監視
3. 複数チームが毎日参照するデータは Apache Iceberg テーブルとしてマテリアライズ（物理化）
4. アクセス頻度の低いデータは仮想のまま維持

これにより，コストとパフォーマンスの最適なバランスを段階的に実現できる．

## ポイント

- データをコピーせずに複数ソースを横断するフェデレーテッドクエリでリアルタイムアクセスを実現
- ストレージコスト削減とデータの鮮度維持が主なメリット，クエリ安定性(データソースへ与えるパフォーマンスなど)がトレードオフ．物理的にデータレイクやデータウェアハウスを持った方が良い場合もある．
- クエリプッシュダウンによりソース側で絞り込みを実行し，データ転送量を最小化
- 「仮想でまず繋ぎ，頻繁に使うものを物理化（Iceberg テーブル等）」がコスト最適化の実践戦略
- [[データファブリック]] の統合レイヤーとして，またデータ民主化のゲートウェイとして機能する

## 関連項目

- [[データアーキテクチャ]] — データ仮想化が適用されるアーキテクチャの全体像
- [[データ基盤]] — データ仮想化が統合する基盤
- [[Apache Iceberg]] — 仮想→物理化の移行先として採用されるオープンテーブルフォーマット
- [[データファブリック]] — データ仮想化を包含するメタデータ駆動のアーキテクチャ
- [[データメッシュ]] — ドメイン分散オーナーシップ．データ仮想化で横断クエリを補完
- [[データフェデレーション]] — データ仮想化のサブセット概念．異種ソースの統合アクセスに特化

## 参考

- [What is Data Virtualization? - Denodo](https://www.denodo.com/en/data-management/data-virtualization)
- [What is Data Virtualization? - Databricks](https://www.databricks.com/blog/what-is-data-virtualization)
- [Data Virtualization and the Semantic Layer - DataLakehouse Hub](https://datalakehousehub.com/blog/2026-02-semantic-layer-07-data-virtualization/)
- [データ仮想化とは - TIBCO/NTTドコモビジネスX](https://www.nttcoms.com/service/TIBCO/products/data-virtualization/overview/)
